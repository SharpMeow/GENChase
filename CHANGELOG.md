# Changelog

## Unreleased

- **Hodgkin-Huxley bistability at J = 8, proved by computer.** `papers/hh-dynamics/code/certify_bistability.py` proves, in Arb ball arithmetic, that for J = 8 uA/cm2 and every leak potential in [10.59, 10.62] a unique, locally asymptotically stable equilibrium coexists with an orbitally asymptotically stable periodic orbit (period in [16.005827509, 16.013912063] ms), and encloses an unstable periodic orbit of saddle type at three leak potentials. 84 checks: 30 proof checks, 24 negative controls, 23 self-tests, 7 numerical-only. An independent reading of the program (the model against the 1952 equations, 13 mutations of the code) found missing outward rounding, a Poincare map that could start off its section and weak controls; all are fixed and all 13 mutations are now caught. The fixes have not had a second reading. `RESEARCH.md` logs the prior-article status, with Guckenheimer and Labouriau (1993) still to read.

- **Prior art review is now prior article review** (owner's decision, 2026-09-26), because GENChase is also an art studio. Item 5 of the papers' quality bar is "Prior article review" in every `papers/*/notes/QUALITY.md` and in `tools/paper-check.js`, whose self-test now also refuses the item under its old name; no item was ticked or unticked. `RESEARCH.md` is the ledger of prior-article searches, and the term changes in `AGENTS.md`, `CONTRIBUTING.md`, the pull request template, the docs, `IDENTITIES.md`, `llms.txt` and `techniques.json` (from `tools/index.js`), the volunteer runner's online search button, and the notes and READMEs that used it. The issue template is `prior-article.yml` ("Prior article", title prefix `[prior-article]`), and three notes are renamed: `papers/collapse-without-rotation/notes/prior-articles-2026-09-25.md`, `identities/sources/prior-article-kill-2026-09-23.md` and `identities/sources/outside-family-prior-article-Pstar-F5-2026-09-23.md`, with every link to them. Dated records, quotations and the released sections of this file keep the words they were written with.

- **Versions are written without a leading v** (owner's decision, 2026-09-26): `0.8.0`, not `v0.8.0`, for the studio and for the papers' companion releases, in tags, `CITATION.cff` (`version: "0.7.1"`), the headings of this file and of `papers/*/RELEASES.md`, and the docs. The "Publish offline studio" workflow and `tools/package-release.py` take a new version only in the plain form, and the release also stops if the version is already tagged under either spelling. The tags made before then keep their names (v0.4.1 to v0.7.1 here, v1.0.0 to v2.1.0 in the companions) and no tag is renamed: the notes-only run finds an old tag from its plain version, `tools/release-notes.py --existing` reads the notes of a v tag from the section without the v and has a `--self-test` (run in CI), and `tools/paper-publish.sh` takes a v tag only for a companion release that exists, to update its notes, and refuses a plain tag whose version the companion already has under its v tag. `tools/paper-publish-check.js` tests all three cases, and `tools/lint.js` fails a `CITATION.cff` version or a heading of this file or of a `RELEASES.md` written with the v.

- **Paper 3 is published as a preprint.** Release 1.0.0 of `ChaseHendrick/stable-expansion` is archived on Zenodo as a preprint with its abstract, doi:10.5281/zenodo.22971173. The DOI is in the paper's data availability paragraph, its README (header and citation), `RELEASES.md`, `papers.json` and the root README, which now lists three released preprints.

- **causticsea simulates Swift–Hohenberg, and its wavelength no longer reads oblique stripes long (recipe v6).** The tab counted zero crossings along rows, which measures λ/|cos θ| for stripes at angle θ to the rows (1.43 times too long at 45°, 1.48 times on an isotropic field); it now reports the power-weighted mean of the 5-point Laplacian symbol (field mean removed), converted to the wavelength of an axis mode with the same symbol, with a delta-method error bar from τ_int along rows and columns. The linear part it coded was −∇²(∇² + k₀²), not the Swift–Hohenberg −(∇² + k₀²)² it displays: that operator grows fastest at 14.26 for Wavelength 10 and lets the uniform mode grow. From recipe v6 it is Swift–Hohenberg, and plates change (finer stripes); recipes older than v6 keep the old operator through `legacy: { 6: { operator: 'pre6' } }` and reprint as they were made (new Operator control, recipe check 100 of 100). The default and Fold lock plates now agree with the preferred wavelength (1.2σ, 0.7σ); the strongly driven presets do not, and the validation record says why. `tools/causticsea-science.js` checks the estimator on exact stripes at six angles, with the old estimator as the failure control, and records both operators' growth peaks.

- **Paper 3 meets the quality bar and is ready (owner's sign-off, 2026-09-26).** `papers/stable-expansion/`: an independent reading of Lemma 1 and Theorems 1 and 2 with their certificates found two must-fix items (the exact circulations -4/5 and 47/35 now follow from the vanishing of the sum of pairwise products of the circulations; the proof states what the Krawczyk test certifies), and three further readings of the fixes (mathematics, program, text) found none; their should-fix items are applied. `verify_stable_expansion.py`: 70 checks, 62 in ball or exact arithmetic, 8 in binary64, 0 failed, with new controls of the five-vortex recipe and of the Krawczyk test. Zbarsky is cited by the numbers of arXiv:1912.10862v2. 20 pages; status ready for its companion release.

- **A neural-field travelling pulse, drafted (owner's decision, 2026-09-26).** `papers/nf-pulse/` holds a computer-assisted proof, in ball arithmetic, of a fast travelling pulse in the Pinto-Ermentrout neural field with a logistic firing rate (gain 20, threshold 1/4) at the fixed recovery rate 1/10, with the speed enclosed in an interval of width 1e-25 about 1.1027477097.... `code/run_all.sh` runs the rest-state, unstable-manifold, block and integration certificates and five negative controls in under a minute and exits with status 1 if a check fails. Not independently reviewed; RESEARCH.md records the prior-article search, and two papers by Zhang remain to be read before any claim of priority.

- **Paper 1's corrected version has its DOI.** Release 2.1.0 of `ChaseHendrick/minimal-winding` is archived on Zenodo as a preprint with its abstract, doi:10.5281/zenodo.22966989. The data availability paragraph, README, `RELEASES.md`, `papers.json`, the submission files and the root README now name it; papers 2 and 3 keep citing 2.0.0, the programs they used.

- **Paper 2 has its DOI.** Release 1.0.0 of `ChaseHendrick/collapse-without-rotation` is archived on Zenodo as a preprint with its abstract, doi:10.5281/zenodo.22969841. The DOI is in the paper's data availability paragraph, its README (header and citation), its `RELEASES.md`, `papers.json` (`codeDoi`), and paper 3's reference to it.

- **Paper 3: Theorem 4 proved in full.** `papers/stable-expansion/` gains Appendix A, which writes out for any number of vortex patches every estimate of Zbarsky's confinement argument: the other patches act on a patch only through the strain of their field, and one renormalizing function built from that strain replaces his functions for the two other patches. An independent adversarial reading of Section 5 and the appendix found no gap; its fixes are in (a constant in the geometric bound, one slip wrongly attributed to Zbarsky, a LaTeX error, the regularity of the centres, the bootstrap at the final time, the count of turns, notation). The appendix also proves the conservation of the pseudo-energy it uses (Lemma 6), from properties of Yudovich solutions cited to sources that were read at the places given (Crippa and Stefani 2024 for the whole plane; Ambrose, Kelliher, Lopes Filho and Nussenzveig Lopes 2015), in place of an unchecked reference; Yudovich (1963) treats bounded domains, so the whole-plane statements cite the whole-plane sources too. The paper is 19 pages and still a draft: its quality record leaves open only a second reading of Lemma 1 and Theorems 1 and 2. It cites Leoncini, El Kettani and Ugalde (arXiv:2609.25989) and points to the companion of paper 2.

- **Hodgkin-Huxley dynamics, a new draft (owner's decision, 2026-09-26).** `papers/hh-dynamics/code/certify_equilibria_hopf.py` proves in ball arithmetic, for the 1952 constants: exactly one equilibrium for every applied current in [0, 200] uA/cm2; exactly two Hopf bifurcations, subcritical (l1 > 0) and supercritical (l1 < 0), with transversal crossing; stability below the first and above the second, and exactly two unstable eigenvalues between them. With the printed leak potential 10.613 mV the Hopf points are at J = 9.7754... and 154.5224...; with 10.5989..., the value that actually makes the resting current zero as Hodgkin and Huxley's Table 3 says it should, they move up by 0.0042... to 9.7797... and 154.5267.... The qualitative statements hold for every leak potential in [10.59, 10.62]. 27 checks, with negative controls and an independent SymPy/mpmath cross-check of l1 to 40 digits. RESEARCH.md records the neuroscience scout and the constants.

- **A hash set from outside is no longer overwritten by the studio.** The studio writes its tab's recipe into the URL 180 ms after a switch (and on Copy link) with `history.replaceState`. A hash set in that window by the address bar, a link or a script was replaced before its `hashchange` event ran, and the event then applied the old tab again. `writeHash` now leaves a hash alone if it changed since the studio last wrote or applied one, and the pending event applies it. `tools/studio-navigation-check.js` reproduces the race deterministically (set the hash, click Copy link before the event), fails on the old engine and passes now. This race made `print-smoothing-browser.js` time out intermittently in WebKit CI.

- **Paper 1 meets the quality bar and is ready again.** Two independent readings of the parts of `papers/minimal-winding/` merged in from the α-model draft (Sections 4 to 8) found no mathematical error and four must-fix items, all fixed: Lemma 6 is stated for collapses, Corollary 2 excludes a vortex at the collision point, Section 8 states and cites the Krawczyk–Moore theorem (Rump 2010, Theorem 13.3) with the trust base, and the angular impulse vanishes by Section 2 rather than by the program. The should-fix items are applied, `certify_collapses.py` gains a rigorous sign check (94 checks), and the record is in `notes/QUALITY.md`. Release notes for companion 2.1.0 are in `RELEASES.md`; the paper is 38 pages.

- **Each paper's Zenodo record is its preprint, with its abstract.** Companion releases were archived as Software. By the owner's decision (2026-09-25) they are archived as Publication, Preprint: the manuscript with the programs that check it. The description in a companion's `.zenodo.json`, which Zenodo shows and OpenAIRE copies, was a one-line placeholder. `tools/paper-sync.js` now writes the `## Abstract` section of the paper's README, with its TeX math turned into plain text (Greek letters, √, ≤, subscripts), followed by one line saying the record holds the manuscript, a preprint not yet peer reviewed, with its programs and their output. Staging refuses a README without an abstract, or one whose abstract keeps TeX with no plain-text form, and the self-test plants both. The records of paper 1 already on Zenodo keep their old type and description until they are edited there.

- **A quality bar for the papers, enforced.** No paper is published or preprinted until its record, `papers/<id>/notes/QUALITY.md`, checks seven items with their evidence: complete proofs (nothing adapted without being written out), rigorous computation, every claim labelled, sources read, a prior article review logged, an adversarial second reading with its fixes, and reproducible programs. `tools/paper-check.js` refuses the status `ready` or later otherwise, and its self-test plants an open item, a renamed item, a missing item and a checked item without evidence. The records are in the papers' `notes/` folders, which stay in GENChase. Paper 2 meets the bar. Paper 1 goes back to `preparing` until the parts merged in from the alpha-model draft have a recorded adversarial reading, which has started; it stays on Zenodo as released. Paper 3 stays a draft until Theorem 4 is written out in full. `docs/PUBLISHING-PAPERS.md` (section 0) and `AGENTS.md` state the rule.

- **Paper 2 is ready for its companion repository and its first Zenodo release.** `papers/collapse-without-rotation/` is marked ready in `papers/papers.json` (owner's decision, 2026-09-25), with release notes for 1.0.0 in its `RELEASES.md` and the data availability paragraph pointing at `ChaseHendrick/collapse-without-rotation`. Aref, Newton, Stremler, Tokieda and Vainchtein, *Vortex crystals* (2003), read for Lemma 1: it does not contain the construction, and the paper now cites it for the translating equilateral triangle and for the triangular-number restriction on translating configurations with circulations of equal magnitude. The README's reproduce list gains the two programs it was missing.

## 0.7.1

The publication date is recorded in the GitHub release notes.

- **Paper 2: sharpness for every number of weak vortices, and O'Neil 2010 read** (draft, 24 pages). `papers/collapse-without-rotation/` proves that the constant sqrt(3)/2 of its Theorem 1(a) cannot be increased for any number of weak vortices (Corollary 3), where only even numbers and three were proved before: Lemma 1 builds a nondegenerate translating cluster of every size from a pair or the triangle (1, 1, -2) by placing weak vortices +-eps at simple stagnation points, and Theorem 3 turns any such cluster into collapses with P tending to sqrt(3)/2. `verify_sharpness_all_n.py` (37 checks) checks the starting clusters exactly and grows clusters of 4 to 15 vortices, and with `--large` one of 101, with their collapse families at forty digits. O'Neil, Theor. Comput. Fluid Dyn. 24 (2010), supplied by the owner and read in full, found collapsing vortex sheets with point vortices first, including the S-shaped sheet between two negative point vortices at P = 1/2; Section 6 now credits him and claims only the direct continuum solution and the minimum P_inf along the family. RESEARCH.md records both.

## 0.7.0

The publication date is recorded in the GitHub release notes.

- **A third vortex paper, drafted** (owner's decision, 2026-09-25): `papers/stable-expansion/`, *Stable Self-Similar Expansion of Four and Five Point Vortices and Confinement of Vortex Patches* (status draft, 12 pages). A computer-assisted proof (Krawczyk test in FLINT/Arb, reusing the minimal-winding certification modules) that the circulations (-1, -5/2, -1/9, 4/5) and (-1, 3/7, 7/8, -9/7, -47/35) have self-similarly expanding configurations that are linearly stable modulo rotation and their one-parameter family, through an exact lemma on the spectrum (six eigenvalues forced by the symmetries and the family, the rest in pairs k, 2 - k). Theorem 3 makes the stability nonlinear: every nearby motion with the same circulations stays within a bounded distance of an exactly self-similar expansion of the family member with its own energy, the relative deviation O(t^(-1/2)); Proposition 1 is the same estimate for motions with a small forcing that decays faster than the velocities. Theorem 4 uses it in place of the one step of Zbarsky's proof (Commun. Math. Phys. 388, 2021) that needs three vortices and carries his vortex-patch confinement theorem to the two configurations; it adapts his single-patch estimates and still needs a specialist's check. `verify_stable_expansion.py` has 58 checks, including the controls and direct integrations of four and five vortices to a 5e8-fold growth. Two second readers checked the new sections. RESEARCH.md records the prior art, including Davila, del Pino, Musso and Parmeshwar (arXiv:2410.18220), a construction of particular solutions near the three-vortex spiral.
- **arXiv deferred** (owner's decision, 2026-09-25): arXiv asked for an endorsement at the first attempt to submit the minimal-winding paper to physics.flu-dyn, so no paper goes to arXiv for now. The Zenodo release 2.0.0 (doi:10.5281/zenodo.22963796) is the preprint of record; the cover letter, the submission checklist, the publishing runbook, the paper READMEs and `papers.json` say so, and the arXiv metadata is kept for later. The earlier note that no endorsement was needed was wrong.
- **A second vortex-collapse paper, drafted** (owner's decision, 2026-09-25): `papers/collapse-without-rotation/`, *Point-Vortex Collapse Without Rotation: A Cluster Mechanism, a Phase Diagram and a Continuum Limit* (status draft, 22 pages), cites the minimal-winding paper as its companion. It proves that a strong vortex with weak tight clusters of any sizes and signs has P >= sqrt(3)/2 - o(1), so no collapse without rotation in that regime for any number of vortices; that one weak triple gives a family of collapses with P = sqrt(3)/2 + (2 sqrt(3) g1 g2 g3/S) gamma + O(gamma^2), below sqrt(3)/2 for signs (+,+,-) at every small strength (implicit function theorem at the degenerate limit, first-order coefficient by exact computer algebra); and that no self-similar collapse with nonzero total circulation is mirror symmetric; numerically it maps the least alpha for a collapse without rotation (least N 11 at alpha = 2, 60 for SQG, limit about 0.66-0.80 rather than 0), the instability of the certified collapses without rotation, and a continuum limit of two point vortices and a vortex sheet whose winding has a local minimum 0.47736353369... along one family, making no claim of priority there until O'Neil (2010) is read. Nine verification programs (336 checks) and their data are in the folder.
- **The alpha-model bound holds for every α > −2, and the SQG collapse without rotation is proved.** The merged paper's Theorem 2 now covers −2 < α ≤ −1 by a new case split (Remark 4, checked by `verify_alpha_below.py`, which replaces `verify_alpha_extension.py`), closing the open problem below −59/40. Theorem 5(b) proves by interval arithmetic (`certify_sqg60.py`) that sixty SQG vortices collapse without rotating, which was numerical before.
- **The two vortex-collapse papers are one** (owner's decision, 2026-09-25). The alpha-model draft is merged into `papers/minimal-winding/`, now titled *Minimal Winding in the Self-Similar Collapse of Point Vortices* (31 pages): the sharp three-vortex bound √(3+α)/(2+α) in the α-models joins the Euler results, and a new section on more than three vortices carries computer-assisted proofs in interval arithmetic (`code/certify_collapses.py`: Euler collapses with P < √3/2 and certified strict local minima for four, five and six vortices, four-vortex minima below the three-vortex bounds at α = 1 and 2, and an α = 2 collapse of eleven vortices without rotation) beside clearly labelled numerical results for up to 603 vortices. The draft's programs, data, figure and notes moved into the paper folder with their history, the checks it cited in `research/` became companion programs (`verify_alpha_equal_circulations.py`, `verify_many_vortices.py`) with their configurations in `data/`, `papers/alpha-winding/` and its `papers.json` entry are gone, and the Typst copy of the manuscript was dropped: the LaTeX source is the only one.

- **Releases carry real notes.** A studio release now takes its notes from its `CHANGELOG.md` section, under "What's new", followed by how to get the studio and where it was built from (`tools/release-notes.py`); the release stops if the section is missing or empty. The "Publish offline studio" workflow gains a **notes only** mode that rewrites the notes of existing releases, one version or `all`, without touching tags or files. A paper's companion release takes its notes from `papers/<id>/RELEASES.md`, which is published with the companion; the publish run refuses a tag without notes and brings the notes of an existing release up to date. `papers/minimal-winding/RELEASES.md` describes 1.0.0 and the changes since.
- **The author's email address appears only in the manuscripts.** The paper READMEs show the ORCID instead, the generated CITATION.cff leaves the address out, and `paper-check` and `paper-sync` refuse it anywhere outside a manuscript.

- The workflows use `actions/checkout@v5` and `actions/setup-node@v5`, which run on Node 24; GitHub is retiring Node 20 for Actions.

- **The stage badge follows a user-typed mode.** In Turing Patterns' Custom reaction mode the badge now reads Unvalidated and the export provenance records `validation: unvalidated`, with the tab's own status in `tabValidation` and the reason in `validationNote`. A module reports such a mode through the new optional instance method `evidence()`, which can only lower the status, never raise it. The tab strip keeps the tab's status.

- The GitHub Pages deploy is off by the owner's choice and runs only when the repository variable `PAGES` is `on`, so a merge no longer fails on a disabled Pages site. The site build still runs as a check.

- **Rough growth: the relaxation rule stops at the top of the lattice.** It was the only column rule without the stop, so on a thin, nearly full lattice particles landed above the last row, where the write was silently dropped while the heights, width and particle count still counted them. Ported from the closed PR #123. All six column presets are bit-identical before and after; the kpz record stays partially validated, because that branch's promotion missed its own pre-registered 5-standard-error test at 16 seeds and passed only in a 64-seed follow-up (`validation/KPZ.md`).

- **Turing Patterns: a Custom reaction mode.** Kinetics now offers Custom reaction: type f(u, v) and g(u, v) in u, v and four slider coefficients a to d, and the tab steps u_t = D_u∇²u + f, v_t = D_v∇²v + g with its own grid, stencils, seeding and picture, and new D_u and D_v sliders. It opens on Schnakenberg's kinetics written out (0.1 - u + u^2*v and 0.9 - u^2*v, D_u = 1, D_v = 100), so a link that only picks the mode runs the tab's own spots. The formulas are parsed by `src/shared/expr.js`: closures on the CPU, and `U.expr.toGLSL` for the step shader; nothing typed is evaluated as code, and a constant GLSL cannot hold is refused as the text is typed. The step is plain forward Euler, held under 0.8 of 2/(λ_D + ρ_J): λ_D = Q c² max(D_u, D_v) from the stencil symbol (Q = 8, or 16/3 for the 9-point), ρ_J the largest eigenvalue magnitude of the finite-difference Jacobian over 2048 samples of the field (the center and the largest cell of each of 32 × 32 blocks), taken every 50 steps and whenever a parameter changes, on the step count alone so a recipe reprints. The status line shows the step, the ceiling and both terms, and says "user-defined, not validated", with no comparison with theory. A field found non-finite stops the plate and says so, and pow, sqrt, log, asin and acos are guarded so a reaction taken outside its domain gives NaN on the GPU as it does in JavaScript. The seeding perturbs a uniform state found by Newton's method. The ceiling is an estimate, not a stability proof; the tab's record in `validation/techniques.json` gains that limitation and `validation/RDX.md` describes the mode. The built-in kinetics, defaults and seeding are unchanged and the new keys are left out of every hash that does not change them, so old recipes reprint as made (`tools/rdx-science.js` 90 of 90 checks and 27 of 27 controls, `tools/rdx-print-state.js` and `tools/art-check.js` rerun unchanged). `tools/engine-api-check.js` covers the status, the refused constant and the stop. Typed reaction terms follow VisualPDE (Walker, Townsend, Chudasama and Krause, 2023), credited in the tab and in RESEARCH.md.
- **Fluid: the listed equation matches the code.** The catalog equation listed a viscous term, nu lap u, that no pass computes. It now reads as the solver does: advection, a constant per-step damping (the dissipation control, a linear drag), vorticity confinement and forcing, and a Jacobi pressure solve that reduces the divergence without removing it; the description says the model is a damped, forced incompressible flow with numerical diffusion from the back-trace, not viscous Navier-Stokes. The fluid record, still unvalidated, carries the correction.
- **The owner's name everywhere: Chase Hendrick, ORCID 0009-0002-9754-6087.** The GitHub account is now ChaseHendrick (formerly SharpMeow), and the retired pseudonyms SharpMeow and Chaos are replaced in the notice, the colophon, the citation and Zenodo metadata, the identities note and its statements, the git identity rule, the workflows and the volunteer runner (which now accepts pull requests on `ChaseHendrick/GENChase`). The ORCID is in CITATION.cff, both .zenodo.json files, the JOSS draft, `papers/papers.json` (so each companion's CITATION.cff and .zenodo.json carry it) and under the author on both manuscripts and the note, whose PDFs are rebuilt at unchanged page counts. The studio's Pages address moves to chasehendrick.github.io/GENChase, because GitHub does not redirect Pages sites after a rename. `paper-sync` now refuses a link into GENChase under either account name.
- **Recipe v5: vegetation's step ceiling is the combined explicit bound.** The water row is forward Euler in diffusion and upwind advection together, so its grid-mode bound is `dt (Q D_w c^2 + 2 v c - 1) < 2`, not the smaller of two separate limits, which overshot it by up to 1.6 and grew a water checkerboard for D_w >= 5 at slope 40. The ceiling is now 0.8 of `2/(Q D_w c^2 + 2 v c)`, with the plant row at the same margin; the default and every preset but flat ground take a smaller step (0.01818 instead of 0.02 at the defaults). The new Step ceiling control records the rule, and `legacy: { 5: { ceiling: 'v4' } }` keeps older links on their old step wherever it was stable, so they reprint (checked by a before-and-after float32 hash), and gives them the combined step where it was not. `tools/pde-order.js` gates it: 103,680 swept states, every Fourier mode, the actual shader from a 1e-6 checkerboard, and every preset under the bound, with the old ceiling as a failure control (`validation/PDE-ORDER.md`, Findings 1).
- **A float16 policy for chemotaxis, amb and pfc.** On a device without float32 color buffers chemotaxis and amb now store their state as the deviation from their uniform state, which ends the chemotaxis freeze (standard deviations 0.98 of float32's at step 1000) and amb's shrinking fluctuations (1.00). pfc refuses the fallback with a message on the stage and in the status line, because neither storage keeps its conserved mean (drift 30 and 9.4 times float32's). `tools/half-float-check.js` holds both to criteria fixed before the runs, with the old fallbacks as failure controls, and `tools/provenance-check.js` checks the export and the refusal (`validation/HALF-FLOAT.md`). Float32 devices compute exactly what they did before.
- **Three-vortex collapse bound: a Custom configuration mode.** Kind now offers Custom, which takes three circulations and three positions from new Configuration controls (exact values through Settings JSON). The status line gives Σ ΓᵢΓⱼ and the angular impulse L about the center of vorticity, the spread of κ = (dz/dt)/(z − z_c) across the vortices, and for a self-similar collapse κ, t_c, the spiral angle arctan 2P and P beside the sharp bound √3/2 of papers/minimal-winding, all through `stats.compare` with basis deterministic. A self-similar configuration draws its exact logarithmic spirals; anything else is integrated with RK4 at a step tied to the closest pair, with the change in the Hamiltonian printed. A Make it collapse toggle computes on the nearest collapsing configuration in the paper's normalization (1, μ, −μ/(1+μ)) on the zero-impulse circle, and a Custom, μ = 1/2 preset sits at the paper's arc minimum P = 1.0647059763. Defaults and the existing modes are unchanged, so old recipes reprint as before; the new keys travel in the hash. `tools/three-vortex-custom-check.js` (in CI) checks the mode's formulas, the projection and two mutation controls; independent high-precision and browser evidence for the custom mode is pending and recorded as a limitation.
- **Type your own equations.** Flow Field gains a "Custom field" mode: type u(x, y, t) and v(x, y, t) and the strokes follow that field instead of the noise, with a time slider for t; it opens on the Taylor-Green vortex array. Attractors gains a "Custom ODE" system: type dx/dt, dy/dt and dz/dt in x, y, z and the coefficients a, b, c, d, integrated with the same RK4 step as the built-in flows; it opens on the Lorenz equations written out, and a divergence guard stops the plate and says so on the status line when a coordinate passes 1e6 or becomes undefined. The formulas travel in the link, so a shared plate reprints. Both modes are labeled user-defined and not validated, print no comparison with theory, and each record in `validation/techniques.json` gains that limitation. The default modes and defaults are unchanged, so every existing recipe reprints as made. Formula entry follows VisualPDE (Walker, Townsend, Chudasama and Krause, 2023), credited in both tabs and in RESEARCH.md.
- **A safe expression language, `src/shared/expr.js` (`Studio.util.expr`).** A tokenizer and a recursive-descent parser build a syntax tree that is compiled to closures, and a GLSL emitter writes the same tree from whitelisted tokens; typed text is never run as code. Numbers, whitelisted variables and parameters, `+ - * / ^` with unary minus (`^` right-associative, `-x^2` is `-(x^2)`), parentheses, 19 functions and the constants pi and e; at most 256 characters and 32 levels of nesting, and every error carries a message and a column. `node tools/expr-check.js`, now in `npm test`, checks agreement with `Math` on seeded points for every function and 23 precedence cases, refuses 97 hostile inputs (script injection, prototype names, Unicode lookalikes, over-long and over-deep text), checks the GLSL output exactly and by round trip, and catches four precedence mutants as negative controls. `tools/lint.js` now fails any `eval(`, `new Function` or string timer in `src/`.
- **A `text` control in the engine (API version 1, additive).** It validates on every keystroke, commits only valid text so the plate keeps the last valid formula, shows the problem and its column as text, and falls back to the default for a bad value in a link; `activeOnly` keeps an inactive formula out of the caption. `docs/ENGINE-API.md` and `tools/modules/CONTRACT.md` describe it, and `node tools/engine-api-check.js` covers it, including a formula containing `<img src=x onerror=alert(1)>`.

- **Recipe v4: lozenge frozen regions connected to the rim.** A rhombus is frozen when a chain of edge-adjacent rhombi of its own orientation joins it to the rim, the analogue of the Jockusch-Propp-Shor polar regions. Plates read low at the offered sizes for a stated finite-size reason; extrapolated over sizes 12 to 48 the free area reaches 0.9005 ± 0.0052 against π/(2√3), while the old radius-3 test lands 6.6σ low. The `ring` key is now the Frozen test control (0 = rim, the default); `legacy: { 4: { ring: 3 } }` keeps older links reprinting as made, and `tools/recipe.js` checks it.
- **Calibrated per-plate error bars on both tiling tabs.** The Aztec polar-fraction bar is now the autocorrelation-time standard error of free cells in 60 angular sectors, replacing the four-region spread that anticorrelated regions made 1.1 to 1.5 times too wide; the lozenge free-area bar uses the same estimator. Scatter over bar is 0.83 to 0.91 (Aztec) and 0.77 to 1.03 (lozenge), inside the band 0.75 to 1.33 fixed before the runs. Aztec and lozenge are validated within stated limits.
- **Random matrices: the top of the color ramp keeps its color.** The palette offset was applied to the ramp position modulo 1, so at the default offset the top level and, in "Color by: row", the last row took the lowest color. The offset now turns the 256-entry ramp in index space; at offset 0 every other level keeps its exact color. `tools/rmt-science.js` reran and passes with its print check's color rule updated, and the old rule put back in the module fails it. No mark moves.
- **You can edit a paper's public repository directly.** The publish papers workflow no longer overwrites a companion repository: it keeps what this repository published on a `genchase-sync` branch and merges that into the companion's main branch, so the owner's direct edits survive every run, and a run whose update touches the same lines as such an edit stops without pushing and names the files. `sh tools/paper-pull.sh <id>` brings direct edits back into `papers/<id>/`. The workflow's commits carry the project identity even when the environment sets another one. `node tools/paper-publish-check.js`, now part of `npm test`, runs both scripts end to end against local repositories (20 checks), and an overwrite-instead-of-merge mutation fails it.
- **Papers in the standard layout, each publishable as its own repository.** Each paper is a folder under `papers/` laid out as a research compendium: `paper/` (the LaTeX manuscript, its PDF and figures, and a Typst copy), `code/` (the verification programs and a `requirements.txt`), `data/` (their output), a README with the abstract, how to reproduce and how to cite, and the working folders `notes/` and `submission/`. The committed PDFs are now the LaTeX builds (`sh tools/paper-build.sh <id>`), the same PDF arXiv builds, and the manuscripts carry the author's contact email and a funding statement. Because this repository may be private, the papers point readers to public companion repositories (now `ChaseHendrick/minimal-winding`), and the **publish papers** workflow (`tools/paper-sync.js`, `tools/paper-publish.sh`) copies a paper there once `papers/papers.json` marks it ready, with a LICENSE, CITATION.cff and .zenodo.json, and locks the repository against edits by anyone but its owner. `node tools/paper-check.js` fails a ready paper that still points into this repository, and `sh tools/arxiv-bundle.sh <id>` writes the arXiv upload. The programs were rerun from their new folders and reproduce their committed output. The private-copy procedure and the endorsement request are gone: the email is public by the owner's decision and no endorsement is needed.
- **Reviews from four older branches merged.** Tilings (exact geometry and print, partially validated; two module defects recorded, not fixed: the P3 matching arcs and the grout inset), Veselago (ray trace and print, partially validated, harness ported to the current module), tennis (rigid-body audit and print, validated within stated limits) and grains (contact mechanics, statics and print). Grains is recorded as partially validated, not validated as its branch proposed, because two criteria fixed before the run were missed: production-step restitution (-10.6% against 10%) and incline slip (+8.67% against 3%). `tools/distribution-check.js` now waits for the studio page before reading `Studio`. Every harness was rerun on the current base and reproduced its recorded results apart from engine fingerprints and color-derived print numbers.
- **Signed release tags removed.** The owner decided that an individual research project does not need them; priority rests on arXiv and Zenodo records. `tools/tag-release.js`, `tools/signing-check.js`, `docs/SIGNING.md`, `identities/allowed_signers` and the tag signature workflow are removed, and the release workflow makes the tag itself. Hash commitments are unchanged.
- **Real GPUs and half precision.** `tools/lib/gl-args.js` is one renderer switch for 66 browser tools: SwiftShader by default, the machine's GPU with `GENCHASE_GL=hardware`, and a refusal when a "hardware" run lands on a software renderer. `npm run gpu:science` runs the GPU tabs' recorded science and print tools and writes `validation/results/gpu/<platform>-<renderer>.json` (`docs/HARDWARE-GPU.md`; also a volunteer-runner job). `tools/half-float-check.js` and `validation/HALF-FLOAT.md` measure the float16 fallback on the eleven pde and rdx tabs: rounding-level differences on most, but chemotaxis freezes, pfc loses its conserved mean and amb's fluctuations shrink, none of which the status line can show; on the fallback the status line now says measurements carry half-float rounding.
- **Kitaev computes its chain.** The tab diagonalizes the Bogoliubov-de Gennes matrix of the chain from mu, t and Delta (Householder and implicit QL) and paints the quasiparticle modes, where it used to draw an assumed profile in which t and Delta never entered. `tools/kitaev-science.js` checks every ring eigenvalue against the exact band, zero modes against the winding number on 1,492 parameter points, the splitting against the localization length and an independent solver, with failure controls; validated within stated limits. Every Kitaev plate changes as a documented solver correction.
- **Order of accuracy for the pde and rdx solvers.** `tools/pde-order.js` and `validation/PDE-ORDER.md`: time order 1 and space order 2 confirmed on 10 of 11 tabs, with manufactured solutions for cahn and turing, and failure controls that break both. Found and not yet fixed: vegetation's step ceiling exceeds the explicit bound for D_w >= 5 at slope 40, and float32 floors from the implicit denominators and PFC's sixth-order operator.
- **Sandpile prints true toppling counts.** The status line counted each pop of the toppling stack as one toppling, although one pop topples a site floor(h/4) times, and in single-source mode it counted only the octant's own cells. Every printed count was low and depended on toppling order: 77,107,818 topplings on the default medallion printed as 9,853,706, and the random drops preset printed 11,709,876 for 11,709,974. "topplings", "largest avalanche" and "mean" are now the true, order-independent counts. `tools/sandpile-science.js` checks each one exactly against independent references, including a second-moment identity for the pile, with the old counter as a failure control. The plates do not change.
- **Percolation and sandpile validated within stated limits.** Their reviews were rerun on the current `lattice.js`, and the records moved from `validation/pending/` into `validation/techniques.json`. The Ising review was rerun in quick mode with identical results.
- **Recipe v3: independent random streams for Ising seeds.** Every seed's Metropolis numbers now come from its own keyed stream, so runs with different seeds are independent; on the old shared stream two seeds could read the same numbers and run identical chains, which `tools/ising-science.js` now shows directly (site agreement 1.0000 shared, 0.5046 keyed). Recipes older than v3 keep the shared stream through `legacy: { 3: { stream: 'shared' } }` and reprint as made; `tools/recipe.js` checks it (85 assertions). The tab declares `fieldCells()`, so its print is not supersampled, and compares |m| with Yang only from a cold start.
- **Aztec: the frozen regions are the theorem's polar regions.** A domino is frozen when a chain of edge-adjacent dominoes of its own type connects it to the boundary (Jockusch, Propp and Shor, as Johansson states it). The share now extrapolates to 0.2141 +/- 0.0014 against 1 - pi/4 (the old local test read 67 sigma high), and the status line prints it through `compare()` with a note on the n^(-2/3) finite-size excess. Its per-plate error bar is conservative, so the record stays partially validated. The "Frozen versus free" plates change.
- **Exact benchmarks for five more tabs.** Ising and random matrices are validated within stated limits; SLE, the Aztec diamond and lozenge tilings are partially validated, each for its stated reason. Random matrices now show the correct eigenvalue weight and Dyson drift and compare the spacing spread with the exact Gaudin-Mehta law.
- **Hash commitments for unpublished results.** `node tools/commit-hash.js <file> --label "..."` records a salted SHA-256 of a private file in `identities/COMMITMENTS.txt` without revealing it, and `--reveal` later publishes the file and salt so anyone can check them with `tools/verify-commitment.js` or a one-line Python command. The salt stays in a private record that the tools refuse to place inside the repository and that CI fails on if it is ever committed. A timestamps workflow anchors each ledger line with OpenTimestamps and completes the proofs, proposing them as a pull request. `docs/COMMITMENTS.md` says what a commitment proves and does not, where unpublished work should live, and what switching the repository between public and private costs.
- **A runbook for publishing papers, and a checker.** `docs/PUBLISHING-PAPERS.md` gives the order for each manuscript (the code's DOI on Zenodo first, then arXiv, the reveal of any commitments, one journal, and the bookkeeping after acceptance) and how to handle several papers at once. `research/submission/papers.json` lists every publication and its status, and `node tools/paper-check.js` checks each one: the title in every source, email addresses in public files, the arXiv abstract's length, the page counts against the PDFs (building the LaTeX when pdflatex is installed), and the Typst and LaTeX reference lists entry by entry. Its self-test plants 15 kinds of mistake and runs in `npm test`. It found that the minimal-winding paper's LaTeX build has 13 pages, not the 12 the arXiv comments gave; the metadata and `PRIVATE-COPIES.md` are corrected and the checklist item is closed.
- **Vortex solver, protocol 2.** The search runs seeds on worker threads (`--threads`), and the block's digest does not depend on the thread count. It is 2 to 3 times faster per seed, from a Cholesky solve, a chord retraction, a reduced Hessian along the tangent directions and a stall rule. Protocol 2 seeds are new starts; protocol 1 files still verify.
- **Where collapse stops rotating.** `--continue`, and the `vortex-threshold` job in the validator, follow a certified minimum as α changes and bracket the α at which it first reaches zero winding, to 10⁻⁴. `--verify` re-derives both ends and the family the branch started from.
  - 69 thresholds are recorded, all certified at both ends: the SQG family for N = 9 to 59 and the Euler family for N = 13 to 30.
  - The thresholds approach a positive limit on this evidence: solving their optimality equations directly (to about 1e-12) and extending the families (SQG-type to N = 73, the Euler-type odd-N branch to N = 79), both extrapolate to α∞ between about 0.66 and 0.80, and fits forcing α∞ = 0 are 50 to 1000 times worse. An earlier reading α*(N) ≈ α∞ + 11/N does not hold beyond N ≈ 35. Numerical, not proved.
- **Sixty SQG vortices that collapse without rotating.** Growing the deepest SQG family reaches P = 0 at N = 60, where the thresholds predicted it. The point passes every binary64 test that applies at P = 0 and converges quadratically at 60 digits. This is strong numerical evidence, not a proof, and its priority is unconfirmed. RESEARCH.md entry Q logs a literature search that found no earlier report of such a collapse for α = 1 or α = 2.
- **Fixes from review.** The threshold verifier no longer trusts the tolerance written in a file, and checks the low-side P and the family label. A block's CPU time is no longer multiplied by its thread count. A run whose winding reaches zero is recorded as stationary rather than stalled. The threshold bracket follows P, so a point that fails one certificate test cannot hide the crossing. Threshold files are re-verified in pull requests. Reruns are compared only on the same protocol and parent.
- The volunteer runner gains three art jobs for the `cahn` and `turing` tabs, run on a contributor's computer through the studio's own recipe and print path: a seed hunt, a deep render of one recipe, and one generation of evolved children. Nothing under `src/` or `dist/` changes, and no recipe or engine API changes.
  - The step count is the recipe's `warmup` with `running:false`, verified from the status before any export. Budgets count active time and never change a plate. A deep render refuses, rather than trims, a request over its budget or over the tab's step maximum, and every art job refuses a print that would need more than half the memory.
  - Candidates are ordered by print-sharpness proxies measured on a central 1024 px crop of the real export at native pixels, at every pixel offset, with the spread over that crop and the four corner crops recorded as each candidate's sampling error. The scores are not a measure of beauty and not scientific evidence, and evolved children are unvalidated recipes. The best candidate and one other are rendered again to measure repeatability, and the ranking is called informative only when the spread between candidates exceeds twice their crop sampling error.
  - Results stay in the job folder with a local gallery page and a sealed checkpoint for Resume. Opt-in sharing adds the recipe list and at most 12 thumbnails, shared byte for byte and refused if they carry metadata; prints are never shared. The volunteer-results workflow checks art submissions structurally.
  - `tools/art-check.js` runs the three jobs end to end on small grids in a new CI job, with negative controls.
- AGENTS.md counted six `rdxCreate` tabs; there are five.
- **Uncertainty is a gate.** A status line that prints a measurement against theory now builds it with `Studio.util.stats.compare()`, which will not print it without a basis: sampled with an error bar (or the stated reason there is none yet), exact, deterministic, or true by construction. `tools/lint.js` fails hand-written comparisons and any `compare()` or `setWitness()` call without a basis.
  - The shared harness `src/shared/stats.js` computes tau_int with Sokal's window, series and field means with honest standard errors, blocking, moving-block and slope bootstraps and the Hill estimator, all resampled from the plate's seed. `tools/stats-check.js` checks it against closed-form answers with negative controls that must undercover.
  - Four audits of every module found 121 candidate comparisons on 65 tabs; `validation/COMPARISON-AUDIT.md` lists how each was classified, the new error bars, the references corrected (photon second-order deflection, meissner exact 1/I₀, kakeya disk, exceptional splitting, spin-ice units) and the problems found and not fixed.
  - Ising reports |m| with a tau_int error bar, reports tau_int itself near T_c, and compares with Yang's exact magnetization below 0.95 T_c at h = 0. The magnetization now counts every site from the float spin field.
  - The structured witness takes optional `basis`, `uncertainty` and `method` (additive, API version 1).
- **Provenance in every file, and data out.** PNG, PDF, TIFF, JPEG and SVG exports and the print-job JSON embed `Studio.getProvenance()`: the recipe link, a build fingerprint, the source file's SHA-256, the validation status, the witness, and the renderer and precision the state used. `Studio.exportData()` and the science report's "Download data (.npz)" give the state as NumPy arrays with `meta.json` (the six `pde` tabs, the five `rdx` tabs and `ising`; other tabs say they do not export state yet). `node tools/run.js` runs a recipe headlessly to an `.npz`. `tools/provenance-check.js` reads everything back from the real export buttons.
- **Validation status on every tab, and a scope pause.** Each tab carries a status glyph, the stage's science-report button names the status, and the module browser filters by evidence without loading the inventory. `validation/scope.json` caps the catalog at 130 while at least half of it is unvalidated; `tools/lint.js` enforces it.
- **Publication and outside review, prepared.** `.zenodo.json` now describes the software (the identities note's metadata moved to `identities/zenodo.json`), CITATION.cff cites the software rather than the note, and `docs/PUBLISHING.md` is the owner's checklist for the DOI, an ORCID and both papers. A JOSS-style draft is in `paper/` with a workflow that builds its PDF. `docs/REVIEWING.md`, `docs/REVIEW-REQUEST.md` and an outside-review issue form make a domain expert's sign-off one step, and `tools/science.js` accepts a validated `reviewers` entry, shown in a new VALIDATION.md column. `research/submission/CHECKLIST.md` lists what the vortex submission still needs, including three reference mismatches between `identities/refs.bib` and the manuscript.
- **Affiliation.** The manuscripts, the identities note and the Zenodo metadata name the author as an independent researcher instead of putting the repository name in the affiliation line; the repository is acknowledged as the home of the code and verification programs. The Typst PDFs were rebuilt and the note's fingerprints updated.
- **A research-grade plan.** `docs/RESEARCH-GRADE.md` records a dated audit of what the project still needs to be research-grade software, in priority order: outside review and publication, the exactly solvable tabs, an uncertainty gate, real-GPU runs, data export and provenance, and scope. RESEARCH.md logs the survey of comparable public projects behind it, with every query, the pages that opened and the hosts that were blocked. No public project combining validation records, citations, seeded recipes and physical print export was found; that negative rests mostly on search snippets.
- **SSH reads its intra hopping.** The SSH tab keyed its intra-cell hopping `v`. That is also the recipe-version key, and `sanitize` writes 2 over it. Every plate opened from a link, a preset, Surprise or a reload therefore ran at v = 2. Since w never exceeds 1.6, those plates never drew an edge mode, and the status printed w/2. Dragging the slider changed the plate on screen, but its link still recorded only `v: 2` and reopened as the trivial picture. The key is now `vIntra`, and the label is still "Intra v". The Topological preset now reads w/v 3.00 with edge modes, and Trivial reads 0.33.
  - **Old SSH links change.** A hash made before this change never stored the intra hopping, so it never reproduced a plate whose slider had been dragged. It now reprints at the default 0.45 with its own w and boundary, so for an open chain with w above 0.49 its pixels change. No `legacy` value is declared, because the default did not move and every SSH hash is recipe version 2. Keeping the old pixels would need recipe version 3, with every old link pinned to a picture its controls never affected.
  - **The plate is now an eigensolve.** The display heuristic drew one analytic left zero mode on every row of a topological plate, and a sine profile independent of v and w otherwise. Its verdict followed from an if-statement. The tab now diagonalizes the finite chain, with Householder reduction and implicit QL, which is an open chain's tridiagonal solve. It draws every eigenmode's |ψ|² as a row, highest energy at the top, each row scaled to its own peak. The mid-gap pair sits in the middle rows and shows both ends. Degenerate pairs are rotated into mirror eigenvectors, so the picture does not depend on how the solver split them.
    - **The status line measures instead of assuming.** It reports the end weight of the two states nearest zero energy, over the tenth of the sites at each end (it was 12%). It also reports their energy against the bulk gap 2|w − v|, and a verdict that can disagree with the winding ν. Critical reads "crossover: ξ 18.5 of 48 cells" at the default 96 sites, and edge modes at 160.
    - **This is a documented solver correction.** Every SSH plate's pixels change. No key, default or preset moves, so no `legacy` entry is declared.
    - **Checked against the independent solve.** `tools/ssh-science.js` now evaluates the module's own solver against the independent Jacobi solve on every preset: eigenvalues within 10⁻¹⁰, |ψ|² within 10⁻⁹, end weights within 10⁻⁹. It adds failure controls: swapped hoppings must read trivial, and a wrongly terminated reference and the retired heuristic must both miss.
  - The Grid slider now stops at 160, where the tab's own sanitize always capped the chain; its top four positions used to change the label and nothing else. Every recipe was clamped to 160 either way, so no plate changes.
  - `tools/lint.js` now evaluates each module block and rejects a control keyed `v`, `seed`, `palette`, `bg` or `id`, and any `v` or `id` in defaults or presets. `tools/registry.js` exposes the sandbox it already used, as `registrations()`.
- **Thirteen more Grid sliders stop where sanitize does**, the same fix as SSH: `anderson` and `kitaev` now run 96 to 160, `aubry` and `vortex` 96 to 192, and `hopf`, `cloak`, `darkroom`, `kakeya`, `gyroid`, `weierstrass`, `veselago`, `boy` and `apollonian` 128 to 224; each slider and its sanitize read one pair of constants, every recipe reprints at the same grid, and lint rule 6c now fails any Grid slider stop that a technique's own sanitize moves.
- **Veselago's rays show at any ray count.** The Field view scaled brightness to the brightest pixel, which is always the focus, where every ray crosses, so one ray sat at 1/rays of the ramp. The "Many rays" preset (48 rays under thermal) lit a ray 3 levels above the ground and failed `check.js` as flat. White is now the 95th percentile of lit pixels, so the source and foci clip at exposure 1, and one ray lands between a sixth and a half of the ramp for 12 to 64 rays. Field-view pixels change on every Veselago recipe; no key, default or preset moves, so no `legacy` value is declared. The Log view and the measured image position are unchanged.
- **Release assets are files, attached once.** A release attached `papers/*/data/*`, which failed on a data subfolder, and two papers ship identical copies of the certification modules under the same asset name. `tools/release-assets.py` lists each paper's PDFs, programs and top-level data files, attaches identical duplicates once, stops on two different files with one name, and runs in CI; data subfolders stay in the source archive and the companion repositories.

## 0.6.2

The publication date is recorded in the GitHub release notes.

- **Volunteer computing for the open winding problems (#141).** The local validator gains two jobs that anyone can run on macOS or Linux, headless or in the app. They keep checkpoints, pause on battery and heat (on Apple Silicon Macs), and share results only on request:
  - `vortex-collapse` searches for self-similar collapses of N point vortices in the α-models (α = 0 Euler, α = 1 surface quasi-geostrophic) with the least winding P = |Im κ|/(2|Re κ|).
  - `vortex-grow` continues the deepest recorded family one vortex at a time.
  - A point is recorded as a certified local minimum only if it passes all of these tests:
    - the similarity equations;
    - first- and second-order optimality on the collapse manifold;
    - the conservation laws;
    - a separately written time integration;
    - the six symmetry eigenvalues of its linearization;
    - the pairing k + k′ = 2 of its stability exponents.
  - The similarity, integration, symmetry and pairing tests each have a control that must fail. The optimality and conservation tests are checked directly. A `zero-winding` status covers P = 0, where the strict second-order test cannot apply.
  - `tools/vortex-precision-check.py` re-checks minima in an independent mpmath implementation, at 60 digits by default (`--dps` sets the precision).
  - The `volunteer results` workflow re-verifies vortex result files in pull requests with the verifier from the base branch. After submissions merge, it opens a pull request that refreshes the leaderboard and `COMPUTE.md`.
  - A "Claim a seed block" issue template lets long runs avoid overlap.
- **Computer time and energy (#141).** Every volunteer job records the CPU time of its command and every process it waited for; detached processes and stopped jobs are not counted. It also records an energy figure: measured from the Linux RAPL counter where one is readable, otherwise an estimate of 1 to 20 W per busy core, labelled as an estimate. `COMPUTE.md` totals the computer time of shared jobs and recorded vortex results, and the README gains a section on contributing computer time.
- **First recorded results (#141).** These are numerical candidates, not proofs, and their priority is unconfirmed:
  - 1,596 seeds from the maintainers' own runs, in about 1 CPU hour. Of the 0.98 h, 0.38 h is wall time standing in for files recorded before CPU accounting existed.
  - For N = 4 and 5, the Euler, SQG and α = 2 minima agree with an independent Python search.
  - Growth reaches P = 0.5215733789 for 30 Euler vortices and 0.1453121018 for 24 SQG vortices.
  - At α = 2 and 11 vortices it reaches P = 0: a self-similar collapse that shrinks without rotating. It was checked at 60 digits in mpmath: similarity residual 2 × 10⁻⁶⁰, conservation laws vanishing, non-degenerate geometry. This is strong numerical evidence, not a proof.
- **Research notes on generalizations of the winding bound (#138)**, in `research/generalizations-2026-09-24/`, with independent re-check scripts and their output.
  - Each result carries a status: proved, derived, exact (SymPy) or numerical evidence. Leads that were not checked are listed separately.
  - `RESEARCH.md` entries O and P log the papers read and the searches made. None of the papers read minimizes or bounds the winding for four or more vortices. The papers read for entry P do not report a collapse without rotation; a dedicated search for non-rotating self-similar collapse has not been made yet.
- **A second draft preprint (#140)**, *A sharp winding bound for the self-similar collapse of three point vortices in the α-models*, as Typst, LaTeX and PDF, with a figure, verification programs and their output.
  - It proves P > √(3+α)/(2+α), sharp and not attained, for every α > −1. It extends the bound to α ≥ −59/40 with one interval-arithmetic step.
  - Its Section 5 reports, as numerical results only, four-vortex collapses below the three-vortex floor for α = 0, 1 and 2.
  - **It is a draft that has not been peer reviewed and is not cleared for submission.** Its companion note lists the reading still owed. The PDF attached to this release does not itself say it is a draft.
- **The first paper cites three more papers (#139)**, each read for the purpose. The Typst and LaTeX sources were edited together and both PDFs rebuilt (13 pages each):
  - Conte and de Seze, a 1980 CEN Saclay report printed in 2015 (arXiv:1511.00069). It writes the zero-impulse collapse as the spiral (1 − t/t_c)^(1/2 − iωt_c), so |ωt_c| = P, and gives the complex rate in closed form for arbitrary circulations before Kimura. It is credited in the Introduction and beside Kimura, and the Discussion says that it, like Aref and Kimura, does not minimize P.
  - Hernández-Garduño and Lacomba (J. Math. Fluid Mech. 2007), who prove that every motion of three vortices ending in a total collision is self-similar; cited where the paper extends Corollary 1 to every collapse.
  - Grotto, Romito and Viviani (arXiv:2307.05133), who select a continuation after collapse by vanishing noise; cited beside Gallay and Šverák's regularization.
  - The Typst reference list grows from 20 to 23 entries and every citation number was remapped; the Typst and LaTeX texts carry the same 80 citations in the same order.

## 0.6.1

The publication date is recorded in the GitHub release notes.

- The paper cites four more papers, each read for the purpose: Borisov and Lebedev (1998), Krishnamurthy, Aref and Stremler (2018), Reinaud, Dritschel and Scott (2022), and a clarification of Leoncini, Kuznetsov and Zaslavsky (2000). None anticipates the results.
  - The Discussion now explains why Krishnamurthy and Stremler's normalized path length equals √(1 + 4P²): the circumcircle passes through the collision point.
  - It contrasts collapse-time minima, which depend on the chosen scale, with P, which does not.
  - It notes that the √3/2 in Leoncini et al. is an energy parameter, and that their fastest collapse has P = 3/2.
- The paper credits Kimura (1987), read in full, for the similarity solution with κ as his complex constant, the zero-impulse circle and its split into collapsing and expanding arcs, and the Remark 2 parametrization: his Eq. (4.4) gives both rates for circulations (2, 2, −1), and their ratio is the Remark 2 formula. He minimizes the collision time rather than P, and the general-μ minimum and the ring results are not in his paper.
  - The studio's Three-vortex collapse bound credit names the same equation, since its closed form is that ratio. Its vortex-family evidence was re-run for the new source hash, with all 12 recipes passing.
- The paper cites Gallay and Šverák (2026), read in full. Their collision solution is (1 − t/T)^(1/2 + is), and |s| = P. The paper cites their proof that every collision is self-similar and their closed-form rates. It also notes that Corollary 1 sharpens their s ≠ 0 to |s| > √3/2; a nonzero s is why a regularized collision is determined only up to a rotation. They do not bound or minimize s.
- The paper credits Demina and Kudryashov (2014), now read in full (bought by the author; two independent readers, a numerical check and two referees). Their Sect. 3 gives the two-ring collapse family, with an optional central vortex Γ₀: at Γ₀ = 0 their Eq. (37) is the circulation condition, and their Eq. (36) is the constant S as a function of the relative rotation. For this family they never say which rotations collapse, separate the two rates or evaluate their ratio (their general Eq. (10) does contain the coefficient whose absolute value is P); they minimize nothing and have no three-vortex results. The Introduction and Section 4 now credit their Eqs. (36)–(37), and the Discussion also says what they do not do; Section 2 cites their form of the similarity solution; the proof of Lemma 2 cites their general necessary conditions.
  - A new remark after Corollary 1: the bound does not carry over to larger systems, because the seven-vortex collapse in their Table 1 (Fig. 1a) has P = 12433/(1240√155) = 0.805 < √3/2.
  - The Introduction's sentence on what every zero-impulse configuration does now cites Gallay and Šverák, Sect. 4.5.2, and Krishnamurthy, Aref and Stremler, both read, instead of Aref 1979, which was not read, and Krishnamurthy and Stremler.
- `verify_general_mu.py` adds checks of that fastest collapse, of Kimura's rates against κ, of Demina and Kudryashov's Eqs. (36)–(37) (exactly and against Biot–Savart for n = 2 to 8), and of their seven-vortex collapse (121 checks in all), with a Table 1 row for each. The Typst PDF is now 13 pages and the LaTeX PDF 12.
- The LaTeX build instructions now say three `pdflatex` runs, which is what settles the cross-references from a clean directory.

## 0.6.0

The publication date is recorded in the GitHub release notes.

- Makes the paper *Minimal winding in the self-similar collapse of three point vortices and of two concentric vortex polygons* ready for submission:
  - Adds a LaTeX version, `research/unequal-mu-n5-floors-2026-09-23.tex`, with the same text as the Typst source, for arXiv and journals. It is in the source archive.
  - Notes that every collapse of three point vortices in the plane is self-similar (Drivas, Khanikati and Khanikati 2026), so the bound |ω₀|t_c > √3/2 covers every three-vortex collapse.
  - Credits Krishnamurthy and Stremler (2018) for the numerical observation that the normalized path length before collapse exceeds 2, which Corollary 1 proves with the sharp constant, and narrows the novelty statement to match.
  - Corrects Table 1: the direct minima on the μ grid agree with the roots of Q to 5 × 10⁻²¹, not 6 × 10⁻²⁵ as 0.5.0 stated, and 13 rational values were checked, not 12. The 6 × 10⁻²⁵ came from a mislabeled metric in the verification program.
  - Every verification claim in the paper now corresponds to a committed check. `verify_general_mu.py` adds 21 checks, for 115 in all: the path length and spiral angle from direct integration, Remark 2, the Section 4 identities for general n, the constants of Proposition 1, and each of the 277 sextics of Remark 1.
- Adds the arXiv and journal submission package in `research/submission/`: metadata, an endorsement request, a cover letter, and a note on the author's private copies, which carry the contact email that this repository deliberately leaves out.
- Records the literature read for the paper in RESEARCH.md (entries J and K) and in the paper's companion note.

## 0.5.0

The publication date is recorded in the GitHub release notes.

- 48 techniques are validated within their stated limits, up from 28 in 0.4.1. Another 3 remain partially validated and 79 unvalidated.
- New numerical evidence for Schrödinger (absorbing boundary and long-time phase), convection (coupled spatial and time convergence), Maxwell dielectric reflection and transmission, molecular equilibration and transport, plasma Landau damping, nonreciprocal interactions (long runs, several parameters), shallow water (resolution, parameters, boundaries, long runs), volume waves (long-time dispersion), Hodgkin–Huxley (parameter domain, long runs), direct gravity (large-N forces, time step and softening), the Gross–Pitaevskii condensate, SSH edge states and KPZ growth scaling.
- Print-state evidence for the excitable, turing, cyclic, chemotaxis and vegetation tabs, and for chladni, gerstner, hasimoto, tennis, eight, photon and bec.
- Adds the paper *Minimal winding in the self-similar collapse of three point vortices and of two concentric vortex polygons* in `research/`, with its source, verification programs and their output; the release attaches the PDF and the verification files. It proves that every self-similar collapse of three point vortices has |ω₀|t_c > √3/2, with the constant sharp, and corrects an earlier note: for circulations (1, 1/2, −1/3) the least |ω₀|t_c is 1.0647…, while 2.2039… is the minimum for one orientation of the triangle only.
- Fixes the SVG escape helper, which escaped nothing, and clears four other code-scanning findings.
- SECURITY.md no longer rules out a bounty.

## 0.4.1

The publication date is recorded in the GitHub release notes.

- First public release in the numbered series.
- Includes the browser art start page, offline ZIP, local checker setup, and contribution guides.
- Includes 28 techniques validated within their stated limits. Another 21 remain partially validated and 81 unvalidated.
- Corrects FPUT mode energy, elapsed-time control and integration, with independent trajectory and complete PNG comparisons.
- Publishes the browser studio through an explicit GitHub Pages Actions workflow, with a traceable deployed commit.
- Future releases use major, minor and patch numbers; dates stay in these notes and GitHub metadata.

The earlier calendar-named release is withdrawn in favor of 0.4.1.
